# Success!

The following system files have been saved with your updated config:
~/.gitconfig
~/.ssh/catbash/git-keychain.conf

Make sure to include the following line in ~/.ssh/config:

Include catbash/git-keychain.conf
