# indicate the home directory by absolute or relative path
set HOME "/home/user"

# {git account} username email host optional:sshkey optional:note
{git account} test test@test.com github.com
{git account} example example@example.com codeberg.org
